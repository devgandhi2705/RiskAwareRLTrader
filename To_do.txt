1. Analysis of all 3 Policy training & their evaluation. Summary doc of all 3 policy performance in different regime & situations. 
2. Create a virtual scenario to test the Agent on how it perform. Like UI & how actual traders will be able to see. Just a small & simple visual look.


3. Send RL Summary Doc(Update again), GitHub link(add final commit with deleting not required files & updating file structuring, change evn folder name), evaluation metrics & analysis(in doc with tables & images), models zip files & dataset mail, readme update.



Missing 2 — Momentum baseline comparison in reward

Right now the agent is not explicitly told to beat the baseline strategies.

We earlier discussed:

reward = portfolio_return − benchmark_return

Example benchmark:

equal_weight_return
or
risk_parity_return

This is called differential reward.

Without it RL may settle for mediocre strategies.